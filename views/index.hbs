<div>
  {{#each items}}
    <div class="c-slice">
      <div class="c-slice__inner-wrapper">

        {{! This element is aria-hidden, since it doesn't really contain }}
        {{! information, just some eye candy }}
        <div class="c-slice__image-and-shape-wrapper" aria-hidden="true">
          <svg width="100" height="100" viewBox="0 0 100 100">
            <circle cx="50" cy="50" r="50" fill="white" />
          </svg>
          <div class="c-slice__image-cropper">
            <img class="c-slice__image-element" src="{{ author.photo }}">
          </div>
        </div>

        <p class="c-slice__body">
          <strong>{{ author.firstName }} wrote <a href="{{ url }}">{{ title }}</a></strong> which was looked at <strong>{{ prettyNum views }} times</strong> in just the past week. <strong class="c-slice__exclamation">{{ randomExclamation }}</strong>
        </p>

      </div>
    </div>
  {{/each}}

</div>
